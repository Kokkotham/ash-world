## 2026-06-25 工作记录

### 灰烬世界网站 — 恢复代码 + 第6/7章 + 第4章表格修复

1. 从 GitHub 恢复代码到 E:/WorkBuddy/灰烬世界/ ✓
2. 规则/2版/ 共 112 个 .docx 转 JSON 到 data/ew_raw/ ✓
3. 运行 build_all_data.py 重建 chapters 0-5 数据 ✓
4. 写了 tools/build_ch6_ch7.py，添加第6章（专业制作与创造，8个子章节）和第7章（材料，10个子章节）✓
5. 修改 page-render.js：content 中以 "<" 开头的字符串作为原始 HTML 输出 ✓
6. 添加 .ew-table CSS 样式 ✓
7. 写了 tools/build_ch4_full.py，完整重建第4章（11个子章节，1475段+268表）✓
8. **重大修复**：重写 table_to_html() 解决 docx 合并单元格渲染问题：
   - 表格列重复5+遍 → 识别合并单元格并去重
   - 能力条目(神术/技能) → .ability-card 语义化卡片（类型|MP|前置|等级|描述|强化增益）
   - 等级进度表(1P-100P) → CSS 双栏显示(.ew-level-progress)
   - 目录小表 → 简洁列表(.ew-toc-table)
   - 普通表格 → 连续相同值自动 colspan
9. 配置 GitHub PAT token，推送成功 (commits: a1ee171, 054b92a)
10. 发现网站实际加载 ash-data-inline.js（内联打包文件），不是直接读 JSON

### 第7章材料渲染修复
- **根因**：`build_ch6_ch7.py` 的 `build_ch7()` 只取 `paras[0]`（第一段），丢弃了所有子分类名和材料模板行
- **修复**：
  - 子分类名（鱼、蔬菜、禽畜肉等）→ `<h3 class="material-category">` 金色标题 + 底部金线
  - 材料模板行（材料模板：材料名称品质【】...）→ `<div class="material-template">` 样式化信息块，不再丢失
  - 顺序修正为：h3分类 → 材料模板 → 表格（与原文一致）
  - 新增 CSS: `.material-category` (金色h3) + `.material-template` (左侧金边信息块)
- 已推送 GitHub (commit 19587a1)
