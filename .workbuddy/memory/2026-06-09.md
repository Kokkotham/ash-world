# 2026-06-09 工作日志

## 粒子特效参数调整
- 将粒子尺寸大幅缩小（近景 4.0→0.50，中景 2.5→0.30，远景 1.2→0.18）
- 将Z范围从 [-10, 1] 改为 [-16, -4]，确保全部粒子在骰子后面
- 粒子数量 4000→3000，降低密度
- 基础透明度 0.45→0.30，更半透明
- Shader高亮半径 3.0→2.0

## 粒子效果大改：火星飞舞 + 热源吹开 + 骰子碰撞
- 粒子基础速度大幅提升：X -0.3~-1.3 → -2.0~-6.0，Y 0.15~0.75 → 0.8~2.8，模拟火星快速飞舞
- 鼠标影响半径 5.0→2.5（减半）
- 力模型改为纯热风吹开：取消涡旋切向力，改为纯径向排斥 + 随机湍流 + Z扩散，力值 200→400
- 骰子碰撞检测：粒子进入骰子半径内会被沿径向推开（DICE_COLLISION_R=2.7, DICE_PUSH_STR=150）
- 鼠标在骰子上方时：raycaster检测阻挡，骰子挡住热量，不影响任何粒子
- 鼠标光晕/拖尾颜色从冷蓝白改为暖橙琥珀色，匹配"热源"视觉
- 出界重置边界 ±18 → ±22，适应更快速度

## 骰子旋转系统重构：钻石角度 + 拖拽限制 + 自动回正
- 新增 REST_ROT_X=0.60 (~34°)、REST_ROT_Y=0.30 (~17°) 作为初始/回正角度（顶点朝上的钻石姿态，参考图3）
- 新增 MAX_TILT_X=0.50、MAX_TILT_Y=0.60 限制拖拽最大偏移范围（参考图1&2）
- 新增 RETURN_SPEED=3.0 控制回正速度，松开鼠标后约 0.3~0.5 秒缓慢回正
- 鼠标/触摸拖拽时实时 clamp rotation，防止超出限制
- 惯性衰减阶段也增加 clamp，防止抛出边界
- 禁用原有的自动旋转（autoRotate=false, autoSpeed=0）
- 移除原有的"不倒翁回正"（基于 getRestQuaternion + slerp），改为 Euler 角度线性插值回正
- 子页面鼠标拖尾：创建 mouse-trail.js 共享文件，所有子页面引入，保持统一的暖橙光标效果
- 鼠标光晕淡出：glowFade 从 1.0 缓慢衰减到 0，约 0.55 秒熄灭，避免瞬间消失

## 骰子旋转系统最终定型：X轴限制 + Y轴无限 + 参考角度
- **初始角度调整**：REST_ROT_X 从 0.60 降到 0.42，匹配参考图片的"较正钻石姿态"
- **纵向 X 轴**：硬边界 ±0.65（MAX_TILT_X），拖拽和惯性都截断，防止翻过去
- **横向 Y 轴**：完全放开，无边界、无回正，360° 无限旋转，保持惯性自转能力
- **回正逻辑**：仅 X 轴回正（REST_ROT_X），Y 轴不回正，避免大角度回退时反向乱转
- 删除所有 Y 轴的 Math.max/min 边界截断，包括拖拽、触摸、惯性阶段
