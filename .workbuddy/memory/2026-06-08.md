## 2026-06-08

- 修复粒子气流影响范围过广问题：
  - `influenceRadius` 22.0 → 10.0（缩小受力范围）
  - `maxForce` 300.0 → 350.0（力度补偿）
  - `swirlStrength` 150.0 → 180.0（涡旋力补偿）
  - Shader `glowRadius` 16.0 → 8.0（视觉高亮同步缩小）
- 重新设计粒子系统「烬火星河 2.0」：
  - ASH_COUNT 350→1400，分层颜色（远景暗紫灰/中景暖暗金/近景高亮暖白）
  - 各粒子独立闪烁相位+频率
  - 自然漂移噪声
- 再次微调粒子系统（反馈：太大、影响范围太广、要在骰子后面）：
  - Z分布 `Math.random() * 18 - 15`（-15~3），70%+粒子在骰子(Z≈0)后方
  - 粒子大小整体缩小：远景 0.03~0.08 / 中景 0.06~0.18 / 近景 0.12~0.18
  - 鼠标交互范围 `influenceRadius` 10.0 → 5.0，`glowRadius` 7.0 → 4.0
  - 力度 `maxForce` 350 → 280，`swirlStrength` 180 → 120
  - 出界Z重置同步调整为新分布范围
- 用户指定最终参数：ASH_COUNT=4000, influenceRadius=5.0, maxForce=150, swirlStrength=50, glowRadius=4.0
- 创建可调参数表：所有粒子参数提取为顶部 `const` 命名常量，零运行时开销
- 创建 skill `param-table-workflow`：固化"调参前/后用表格沟通"的好习惯，已打包到 param-table-workflow.zip
