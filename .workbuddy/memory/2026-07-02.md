# 灰烬世界 Ash World — 工作进度交接

> 最后更新: 2026-07-02

## 项目概述

灰烬世界（Ash World / EW）是一个 TRPG 世界设定网站。用户是 DM（主持人），正在将 67 个 .docx 规则书文档逐步转化为数据驱动的 Web 页面。

## 部署地址

- **CloudStudio**: https://9a465c3434074b28a0eebbac66865030.app.codebuddy.work
- **GitHub Pages**: https://kokkotham.github.io/ash-world/ (当前 404，需在 Settings→Pages 手动恢复，选 main 分支 /root)
- **GitHub 仓库**: github.com/Kokkotham/ash-world

## 技术栈决策

- **静态 HTML + vanilla JS**（用户明确拒绝 React/Vite/TypeScript）
- 数据驱动范式：`data/*.json` + `data/page-render.js` 渲染引擎
- 零 npm 依赖
- Python (python-docx) 用于 docx 批量转换脚本

## 已完成工作

### EW 规则书 Web 化 (T01-T06 完整流程)

**数据层** (`data/` 目录):
- `races.json` — 原有 4 个远古种族 + 新增 `players` 数组（16 个玩家种族，每条含属性/特质/描述）
- `professions.json` — 11 大类专修骨架，**已填充**: instinct (本能22条) + knowledge (知识12条)
  - 剩余 9 类待填充: communication/art/survival/special/profession_craft/weapon/combat/martial/arcane
- `divine-arts.json` — 2 神系骨架（父神系/母神系），内容待填充
- `story-rules.json` — 4 类故事运作骨架（combat/social/management/status_effects），内容待填充
- `glossary.json` — **已填充 62 条术语**（核心系统29+种族20+神术13）
- `chapters.json` — 完整章节树（ch0-ch5，含所有子节 + data_source 绑定）
- `links.json` — 25 条交叉关联
- `ew_raw/*.json` — 65 个 docx 原始提取文件
- `glossary_candidates.json` — 502 条术语候选清单

**渲染引擎**:
- `data/page-render.js` — 核心渲染引擎，新增 renderRules/renderGlossary/renderPlayerRaces/renderLevelTable
- `data/ash-data.js` — 数据加载器，已扩展加载 10 个 JSON 文件
- `data/keyword-tooltip.js` — 关键词悬浮提示（IIFE，扫描 `[data-keyword]` 属性）

**页面**:
- `pages/rules.html` — 规则书阅读器（缩略图导航 + 分章滚动 + 下一章按钮）
- `pages/character-sheet.html` — 角色创建页（EW 4属性 1d4 + 10点手动分配双模式，玻璃质感）
- `pages/glossary.html` — 术语表（从 glossary.json 动态渲染）
- `pages/races-ontology.html` — 种族页（远古种族 + 16 玩家种族）
- `pages/common.css` — 共享样式（+tooltip/+表格/+侧栏/+导航栏玻璃质感）
- `pages/rules.css` — 规则书阅读器专用样式

**工具**:
- `tools/docx2json.py` — Python docx 批量转换脚本
- `tools/build_glossary.py` — 术语表生成脚本
- `tools/requirements.txt` — python-docx>=1.1.0

**导航栏** — 已在 `index.html` 和 `pages/common.css` 同步更新:
- 局部下拉面板（`position:absolute`，紧贴触发项）
- 玻璃质感（`backdrop-filter: blur(20px)`）
- 三角指示器 + 外侧光晕 + hover 桥接
- 链接 hover 侧边金条动画 + 文字发光

## 待完成工作（优先级排序）

### P0 — 内容填充
1. **继续填专修数据** — professions.json 还有 9 个分类是骨架
   - 数据源：`data/ew_raw/` 下对应的原始 JSON
   - 格式参考：已填充的 instinct/knowledge 分类
2. **填神术数据** — divine-arts.json（父神系/母神系）
3. **填故事运作数据** — story-rules.json（战斗/交流/经营/状态词缀）

### P1 — 交互增强
4. **给页面内容加 data-keyword 标记** — 在 races-ontology.html / rules.html 的渲染文本中嵌入 `<span data-keyword="hp">` 等标记，让 tooltip 悬浮提示真正亮起来
5. **术语表补充** — 从 glossary_candidates.json 中继续筛选更多术语

### P2 — 修复
6. **数据截断** — races.json 中 木林精"远古精魄"、混血者"有家之人" 描述被截断，需从原始 JSON 补全
7. **version_status 清除** — 发版后统一改为"稳定"
8. **GitHub Pages 恢复** — 需手动操作（Settings→Pages→Source: Deploy from a branch, main, /root）

## 用户偏好

- 使用截图反馈（非代码问题，用户不写代码）
- 先出一个样例确认再批量铺开（避免返工）
- 回答偏好：直接、带对比表格、避免过多技术细节
- 所有新产出放 E:\workbuddy（但当前项目在 C:\ProgramData\...\）
- 决策受「感受」影响最大 — 回答如果感觉不对，请直接说明理由，不要硬推方案
- 用户自称「编创者」。
- 自评AI使用能力「刚刚起步」 — 核心痛点是只能通过「运行后看效果」发现bug，无法预判代码质量。

## 文件索引

- 项目根: `C:/ProgramData/WorkBuddy/chromium-env/13613ht/WorkBuddy/2026-06-07-11-51-51`
- docx 源文件: `E:/Desktop/跑团文件/规则/EW/`
- Python 环境: `C:/ProgramData/WorkBuddy/chromium-env/13613ht/.workbuddy/binaries/python/envs/docx-extract/`

## CloudBase 接入进度 (2026-07-01 ~ 07-02)

### 已完成
1. 编创者在腾讯云 CloudBase 开通环境 `ew-prod`，环境 ID：`ew-prod-d0gciqy00757cc7dc`
2. 在 CloudBase 控制台开启了「匿名登录」
3. 用 browserify 将 `tcb-js-sdk` 打包为浏览器可用的 UMD 文件 `js/tcb-sdk.js` (683KB)
4. `index.html` 引入本地 SDK 文件，并实现匿名登录/登出逻辑
5. 导航栏右侧新增「进入余烬」按钮，登录后变为绿色「余烬行者」

### 待完成
- 测试匿名登录是否真正成功（F12 控制台查看日志）
- 接入手机号登录（需要 CloudBase 短信服务配置）
- 实现角色卡存储功能（登录后可将角色卡存到 CloudBase 数据库）
- 将网站部署到 CloudBase 静态托管（可选，目前仍在 GitHub Pages）

### 技术决策记录
- ❌ 放弃 CDN 引入 SDK（jsdelivr/unpkg 上 tcb-js-sdk 无 UMD 文件，全部 404）
- ✅ 采用本地打包 SDK（`browserify` 打包 `tcb-js-sdk` 为 UMD）
- ✅ 采用匿名登录作为第一版（降低用户注册门槛）
- ⚠️ 当前网站部署在 GitHub Pages，与 CloudBase 不同域，需注意 CORS 配置

---

## 2026-07-02 下午 — GitHub Pages 自定义域名成功上线

- 在腾讯云 DNS 控制台为 `ember-world.site` 添加记录：
  - `@` → A 记录 `185.199.111.153`（1 条即可，免费版限制）
  - `www` → CNAME 记录 `kokkotham.github.io`
- 在 GitHub Pages 设置页填入自定义域名 `ember-world.site`。
- DNS 检查通过，网站已成功通过 `https://ember-world.site/` 访问。
- HTTPS 证书已自动签发，无需手动购买或配置 SSL。
- 最终方案确定：
  - **前端/域名**：GitHub Pages 免费托管 + 腾讯云 DNS 免费版
  - **后端/数据库**：CloudBase 默认域名（`https://ew-prod-d0gciqy00757cc7dc-...tcloudbase.com/`）跑匿名登录和后续数据库

### 费用总览

| 项目 | 费用 | 说明 |
|------|------|------|
| GitHub Pages | 免费 | 静态站点托管，支持自定义域名和 HTTPS |
| 腾讯云 DNS 免费版 | 免费 | 仅支持 1 条 A 记录/子域名，灰烬世界够用 |
| 域名 `ember-world.site` | 10 元/首年 | 腾讯云注册，实名认证已通过 |
| CloudBase 免费版 | 免费 | 匿名登录 + 数据库基础额度 |

---

## 2026-07-02 下午 — 手机号登录功能实现

### 代码修改
- `index.html` 新增手机号登录弹窗（login-overlay），含手机号输入、验证码获取（60秒倒计时）、验证码输入、登录按钮
- 登录弹窗采用暗金玻璃质感风格，与网站整体设计一致
- 替换原有匿名登录逻辑为手机号验证码登录（CloudBase v2 Auth API）
  - `app.auth.getVerification({ phone_number })` — 发送验证码
  - `app.auth.signInWithSms({ verificationInfo, verificationCode, phoneNum })` — 验证码登录
- 已登录用户显示"余烬行者"（绿色）；未登录显示"进入余烬"，点击弹出手机号登录窗口
- 所有 CloudBase API 调用均包含 .catch() 错误处理和用户可见错误提示

### 费用与限制
- 首月 100 条短信免费
- 同一手机号 30 秒内最多 1 条，每天最多 10 条
- 默认短信模板不可自定义（可后续接入外部短信通道）

### 待完成
- 线上测试退出登录 + 编辑资料功能
- GitHub Pages push 暂时网络不通，先用 CloudBase 静态托管测试
- 角色卡数据库存储设计

### 2026-07-02 晚 — 添加退出登录 + 编辑资料功能
- 已登录状态下点击「余烬行者」弹出下拉菜单（退出登录 / 编辑资料）
- 编辑资料：可设置显示名称（存 localStorage）
- 退出：调用 `auth.signOut()` 清除登录态
- 重新部署到 CloudBase，CDN 缓存需 1-2 分钟刷新



### 域名
- 编创者在腾讯云购买域名 `ember-world.site`，首年 10 元，当前实名审核中。

### 部署到 CloudBase 静态托管
- 在 CloudBase 控制台 **HTTP 网关** 添加路由 `/` → 静态网站托管，解决默认域名 418 问题。
- 默认域名可访问：`https://ew-prod-d0gciqy00757cc7dc-1449294861.ap-shanghai.app.tcloudbase.com/`
- 已确认首页内容返回正常（HTTP 200）。

### 修复匿名登录按钮
- 问题：匿名登录成功后，再次点击按钮会执行 `signOut()`，CloudBase 报错「匿名用户不支持登出操作」。
- 修复：修改 `index.html` 登录按钮点击逻辑，已登录状态下不再执行登出，仅作为状态展示；未登录时才执行匿名登录。
- 已通过 `node -c` 校验修改后的 JS 语法。
- 已将更新后的 `index.html` 复制到 `_deploy_live/` 目录，等待重新部署。

### 待完成
- 域名实名审核通过后，将 `ember-world.site` 绑定到 CloudBase 静态托管。
- 测试线上匿名登录是否真正成功（按钮变绿）。
- 后续接入手机号注册 / 角色卡数据库存储。

### 2026-07-02 晚 — 升级 CloudBase SDK 到 2.0+

- 线上测试发现 `tcb-js-sdk` 1.x 返回错误：`[ACCESS_TOKEN_DISABLED] 请升级 js sdk 到 2.0 及以上版本`。
- 本地安装 `@cloudbase/js-sdk@3.6.2`，将其 `dist/index.cjs.js` 包装为 UMD 格式后替换 `js/tcb-sdk.js`。
- 更新 `index.html` 初始化与登录逻辑：
  - 全局变量由 `tcb` 改为 `cloudbase`
  - `app.auth()` 改为 `app.auth`（auth 为对象而非函数）
  - 增加 `unwrap()` 兼容 `{ data, error }` 返回格式
  - 添加 `region: "ap-shanghai"`
- 已通过 `node -c` 语法检查。
- 重新部署到 CloudBase，38 个文件全部上传成功，默认域名返回 HTTP 200 OK。

### 2026-07-02 下午 — 修复 SDK 加载失败

- 线上测试出现「SDK 加载失败」：手动将 `@cloudbase/js-sdk` 的 `index.cjs.js` 包装为 UMD 时，因该文件是已执行的 Webpack IIFE 且依赖外部 `require`，包装后没有正确导出全局 `cloudbase`。
- 解决：从官方 CDN `https://static.cloudbase.net/cloudbase-js-sdk/latest/cloudbase.full.js` 下载完整浏览器版 SDK（已内置所有依赖），替换 `js/tcb-sdk.js`。
- 重新执行 `tcb hosting deploy _deploy_live -e ew-prod-d0gciqy00757cc7dc`，38 个文件全部上传成功，默认域名返回 HTTP 200 OK。

### 2026-07-02 下午 — 线上匿名登录测试成功

- 打开 `https://ew-prod-d0gciqy00757cc7dc-1449294861.ap-shanghai.app.tcloudbase.com/` 测试。
- 点击右上角「进入余烬」后，按钮成功变为绿色「余烬行者」，说明 CloudBase 匿名登录已正式跑通。
- 网站部署、域名路由、SDK 加载、匿名登录整条链路已打通。

### 2026-07-02 下午 — 自定义域名绑定遇阻

- 域名 `ember-world.site` 实名审核已通过，服务状态「正常」。
- 在 CloudBase HTTP 网关 → 域名管理 → 添加自定义域名时，提示需要升级至「云开发标准版」，费用约 179 元/月。
- 结论：CloudBase 免费个人版不支持自定义域名绑定，需选择替代方案或付费升级。

### 待完成
- 与编创者确认是否使用替代方案：GitHub Pages 免费自定义域名 + CloudBase 默认域名跑后端，或其他方案。
- 后续接入手机号注册 / 角色卡数据库存储。

---

## 2026-07-02 晚 — 三阶段用户系统完成并部署

### 架构升级
- 提取共享 CloudBase 初始化模块 `js/ash-init.js`（全局 window.ashApp / window.ashAuth）
- 创建数据库操作模块 `js/ash-db.js`（users / characters / sessions 三集合完整 CRUD + 头像上传）
- 所有页面统一引用链：tcb-sdk.js → ash-init.js → ash-db.js

### 阶段一：数据库打通 + 个人主页
- 新页面 `pages/profile.html`：头像上传（base64降级）、显示名称/简介编辑弹窗、数据统计面板
- 用户资料存 CloudBase `users` 集合，localStorage 降级缓存
- index.html 导航栏升级：共享模块重构、加个人主页入口

### 阶段二：角色卡云存储
- `pages/character-sheet.html` 改造：角色名输入框 + 「保存到云端」/「保存成功」按钮
- 个人主页展示「我的角色卡」列表（种族/专修/等级/日期），点击跳转角色卡页
- 角色卡存 CloudBase `characters` 集合

### 阶段三：用户系统完整体验
- 个人主页「游戏记录」面板：输入团名+身份（DM/玩家），提交存到 `sessions` 集合
- 数据统计面板（角色卡数 / 游戏记录数 / 成就点数占位）
- 完整登录→编辑资料→创建角色→记录游戏全链路

### 部署
- CloudBase 静态托管：42 文件全部上传成功
- 线上地址：https://ew-prod-d0gciqy00757cc7dc-1449294861.tcloudbaseapp.com
- Git 已提交，push 后台进行中

### 剩余
- GitHub Pages 域名 https://ember-world.site 需同步刷新
- CloudBase 控制台可手动建集合索引（首次使用自动建）
- 完整链路线上测试：登录 → 个人主页 → 创建角色卡 → 保存游戏记录

